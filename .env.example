DEEPSEEK_API_KEY=

# Optional — enables the distributed (Upstash Redis) rate limiter in
# lib/rateLimit.ts. Without these, it falls back to a per-instance
# in-memory limiter automatically; nothing else to configure either way.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
